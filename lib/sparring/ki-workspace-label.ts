import { PRODUCT_LABEL } from "@/lib/product-labels";

/** Maintab- und Seitenbezeichnung für den KI-Arbeitsbereich (Routen bleiben `/sparring`). */
export const KI_WORKSPACE_LABEL = PRODUCT_LABEL.ki;
